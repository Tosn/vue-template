import { LOGIN, LOGIN_OUT, GET_USER_INFO } from '@/api/user'
import settings from '../../settings'
const state = {
  token: ''
}

const mutations = {}

const actions = {
  login () {}
}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
